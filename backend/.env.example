# Server Configuration
PORT=5000
HOST=0.0.0.0
NODE_ENV=development

# Email Configuration (Optional - for sending passcodes)
# Option 1: Gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Option 2: Custom SMTP
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password

# Note: If no email config is provided, the app will use a test email service
# and display preview URLs in the console for development
